import {
    StyleSheet,
} from 'react-native';

export default StyleSheet.create({
    container: {
        marginTop: 10,
    },

    title: {
        marginLeft: 5,
        fontFamily: 'Futura',
    },

    list: {
        marginTop: '4%',
        marginBottom: '-55%'
    }
});
